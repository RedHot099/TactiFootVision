name: yolo_model_smoke
paths:
  input: data/soccernet_dummy/img1
  output_dir: results/model_smoke/yolo
detection:
  backend: yolo
  checkpoint: models/yolo11m.pt
  confidence: 0.3
  nms: 0.5
tracking:
  backend: fake
